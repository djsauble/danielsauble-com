---
import { Image } from 'astro:assets';
import './timeline_item.css';

const { title, description, to, image } = Astro.props;
---

<div>
  <div class="timeline-content">
    <h3>{title}</h3>
    <p>{description}</p>
    {image && (
      <a href={to}>
        <Image src={image} alt={title} />
      </a>
    )}
  </div>
</div>